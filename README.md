# Diabetic Retinopathy Classification Web App

## Instructions to Run `web_app.py` Locally  

### 1️⃣ Create a Virtual Environment (Optional but Recommended)  
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python web_app.py
